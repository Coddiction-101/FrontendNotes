# 📦 What's Inside

## Part 1 — JavaScript (18 Sections)

- Variables (var/let/const with the full comparison table)
- Type coercion traps ([] + [], == vs ===, falsy values)
- All function types — declarations, expressions, arrows, IIFEs, HOFs
- Closures with the classic var-in-loop interview trap + 3 fixes
- Hoisting, Temporal Dead Zone, Scope Chain
- Arrays — every method with examples (map, filter, reduce, flat, flatMap...)
- Objects — this binding, call/apply/bind, optional chaining
- Async — Callbacks → Promises → async/await → Fetch API
- ES6+ — Symbols, Map/Set, Generators, WeakMap
- Event Loop — microtasks vs macrotasks execution order
- Design Patterns — Singleton, Observer, Factory

## Part 2 — React (17 Sections)

- JSX rules and gotchas
- Functional vs Class components (with lifecycle methods)
- All Hooks — useState, useEffect, useRef, useMemo, useCallback, useReducer, useContext, useTransition, useDeferredValue, useId, useLayoutEffect
- Context API — complete auth system example
- React Router v6 — nested routes, params, protected routes, query strings
- Redux Toolkit + Zustand — both covered with real examples
- Performance — React.memo, code splitting, virtualization
- Custom Hooks — useFetch, useLocalStorage, useDebounce, useClickOutside, useWindowSize
- React Patterns — Compound Components, Render Props, HOCs
- Testing with Jest + React Testing Library
- 20 Interview Q&As each for JS and React
- 12 coding challenges — debounce, throttle, curry, memoize, EventEmitter, Promise.all
